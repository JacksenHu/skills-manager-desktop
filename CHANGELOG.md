# 更新说明

本文件是**发布说明的唯一来源**：打 tag 时 CI 会取这里对应版本的段落，作为 GitHub Release 的说明（应用内「更新」页的「更新日志」弹窗读的也是它）。

格式约定（Keep a Changelog 简化版）：

- 每个已发布版本一个 `## [x.y.z] - YYYY-MM-DD` 段落；版本号必须与 `package.json` 的 `version` 一致。
- 尚未发布的改动写进 `## [未发布]` 段落；发版时把它改名成新版本号 + 日期，并新开一个空的 `[未发布]`。
- 分类固定用：`新增` / `优化` / `修复` / `其他`（没内容的小节可以不写）。
- 写**用户能感知的行为变化**，不要写内部实现细节。

校验与预览：

```bash
npm run check:changelog        # 校验当前 package.json 版本在这里有段落（verify:all 已包含）
npm run notes                  # 本地预览当前版本的更新说明（= 将发到 Release 的内容）
```

## [未发布]

### 新增

- 新增「SkillsBot」标签页：免登录浏览并安装 skillsbot.cn 的技能。支持分类（含两级子分类与「常规 / 物理 AI」两套分类体系切换）、关键词搜索、热门、最新；技能详情弹窗可直接查看完整 SKILL.md 正文并一键安装进共享库，装完立即出现在「技能库」里。
  - 安装方式为**免登录**：直接把站点公开接口返回的完整 SKILL.md 写入共享库，**不需要注册或登录任何账号**。
  - 站点另把每个技能打包成 ZIP，但 ZIP 下载接口整体要求登录，因此对「包内含脚本等附加文件」的技能会显示**「含附加文件」**角标，并在详情里列出正文引用到的附加文件名，安装结果里也会明确提示本次只写入了 SKILL.md 正文 —— 不会把装不全含糊成装好了。
  - 「可完整安装」的技能（正文即全部内容，约占多数）会明确标注为完整安装。

> 状态：功能代码与接线已完成并通过离线冒烟，**本轮暂不发布**（等真机联网验证后再发）。

## [0.4.0] - 2026-09-21

### 新增

- 顶栏新增「任务日志」入口：安装 / 移除 / 翻译 / 生成路由 / 建拆联接 / 更新升级等操作统一记录，进行中的按钮图标带红色数字徽标；弹窗里可展开看每一步日志、一键清空已完成记录。

### 修复

- **「生成路由」再也不产生调不到的假技能**：路由技能改为写到各 Agent 的**真实技能加载目录**（此前写在共享库根，技能库里看得见、会话里调不到），清单只收录该目录下实际可见的技能；目录名与技能名统一为 `skill-router`，旧版遗留的 `router-guide` 自动清理。
- WorkBuddy AI 桌面版的技能目录补进预设（`.workbuddy-ai\skills`，此前缺少，导致生成的路由技能对该软件无效）。

## [0.3.0] - 2026-09-21

### 新增

- 接入备份的完整生命周期：接入前自动备份、拆除后提示恢复、卡片上一键还原。
- 探测页可直接管理 Agent 配置；技能卡片支持内联编辑。
- 技能库新增来源筛选。

### 修复

- 重复加载风险判定：同一技能根才算重复，不同软件各自的专属目录不再互相误报。

## [0.2.8] - 2026-09-21

### 修复

- 重复加载风险按软件目录段归并（`.xxx\skills` 这类点号开头的目录此前匹配不到，导致漏报）。

## [0.2.7] - 2026-09-21

### 修复

- 重复加载风险改为要求「同一个技能根路径」才算重复，与用户直觉一致。

## [0.2.6] - 2026-09-21

### 新增

- 技能套件：支持从市场成套安装。
- 单个技能可单独启用 / 停用。

### 修复

- 设置弹窗内容过长时无法滚动。

## [0.2.5] - 2026-09-21

### 新增

- 来源自动匹配：为没有来源记录的技能自动匹配 SkillHub 页面并建立版本基准。
- 市场来源的技能也纳入更新检测与一键升级。

## [0.2.4] - 2026-09-21

### 新增

- 接入 SkillHub 技能市场：导航新增「市场」页，支持排序标签、分类筛选、关键词搜索、详情弹窗与一键安装（下载后做 sha256 校验）。

## [0.2.3] - 2026-09-20

### 新增

- 技能库搜索框（可设置只搜技能名 / 也搜简介）。
- 路由技能在技能库里固定归入「Agent 与技能管理」。

### 修复

- 重复加载风险检测：不同软件的专属目录不再互相报警（如 Claude 与 CodeBuddy 各自的目录）。

## [0.2.2] - 2026-09-20

### 新增

- 接入前自动备份原技能目录（可在设置里开关与指定备份位置）。
- 补来源时可直接搜索 GitHub 仓库并点选回填。
- 关于页附开源仓库链接。

### 修复

- 接入确认弹窗内容过长时溢出。

## [0.2.1] - 2026-09-20

### 修复

- 修复本机网络环境下 GitHub 接口报「不可查 / 网络异常」：网络层失败时自动放宽 TLS 校验重试一次（有明确 HTTP 状态码的错误不重试）。

### 优化

- 路由技能在技能库里标注「需手动启用」，技能库顶部与生成日志里都给出启用说明。
- 发布产物命名规范化，与自动更新配置一致（此前会导致更新下载 404）。

## [0.2.0] - 2026-09-20

### 新增

- 技能分类改为可手动指定，并支持自定义分类的创建 / 重命名。
- 来源索引：为技能补 GitHub / skills.sh 链接并建立版本基准，卡片上显示版本与提交短码。
- 设置改为弹窗（右上角齿轮），联接页把「已联接未配置」的入口一并展示。
- 更新页新增更新日志弹窗。

### 修复

- 中英混合的技能简介翻译不完整（现在只翻英文部分，已是中文的跳过）。
- 探测卡片支持编辑标识名与路径。

## [0.1.0] - 2026-09-20

首个版本。

### 新增

- 联接管理：一次性探测 24 类 Agent 的技能目录，一键接入（自动迁移原内容并去重）/ 拆除 / 归并同源多根，介入前先出预案。
- 技能库：列表 / 分类 / 从 GitHub 或 skills.sh 安装 / 移除 / 简介翻译 / 生成总路由技能。
- 更新检测：按来源仓库比对提交，一键升级；工具自身也会自检新版本。
- 设置 / 主题（跟随系统）/ 关闭到托盘 / 配置导入导出。
- Windows 安装包（NSIS）与自动更新；推 tag 自动构建发布。
